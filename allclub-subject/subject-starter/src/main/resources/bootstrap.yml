spring:
  application:
    name: all-club-subject
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: 123456
        group: DEFAULT_GROUP
        # public这个命名空间没有命名空间ID，这个要注释掉，不然会一直循环打印
#        namespace: public
        file-extension: yaml
        prefix: ${spring.application.name}
      discovery:
        enabled: true
        server-addr: 127.0.0.1:8848
        username: nacos
        password: 123456
