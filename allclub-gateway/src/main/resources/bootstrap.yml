spring:
  application:
    name: all-club-gateway
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: 123456
        group: DEFAULT_GROUP
        file-extension: yaml
        prefix: ${spring.application.name}
      discovery:
        enabled: true
        server-addr: 127.0.0.1:8848
        username: nacos
        password: 123456